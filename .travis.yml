language: node_js
node_js:
  # Latest stable node release
  - "node"

os: linux

matrix:
  include:
    - name: test
      before_install: cd client && yarn
      script:
        - yarn test
    - name: lint
      before_install: cd client && yarn
      script:
        - yarn lint

cache: yarn
